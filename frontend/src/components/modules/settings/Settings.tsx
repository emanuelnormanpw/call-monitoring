const Settings = () => {
  return (
    <div className="rounded-card border-border bg-card border p-6 shadow-xs">
      <h2 className="text-ink text-[18px] font-bold">Settings</h2>
      <p className="text-ink-2 mt-1 text-[13.5px]">
        Configure SIP trunk connections, Webhook endpoints, and monitoring
        thresholds.
      </p>
    </div>
  );
};

export default Settings;
