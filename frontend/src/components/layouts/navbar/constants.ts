import type { NavItem } from './types';

export const NAV_ITEMS: NavItem[] = [
  { label: 'Dashboard', to: '/', end: true, inDrawer: true },
];

export const DESKTOP_MEDIA_QUERY = '(min-width: 769px)';
